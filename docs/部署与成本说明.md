# 部署与成本说明

## 不接后端（推荐起步）

- 优点：零服务器成本、开发快、离线可用
- 缺点：无法自动跨设备同步
- 适合你当前场景：固定手机现场使用

## 接后端（可选）

- 优点：可跨设备同步、可做多人共享数据
- 缺点：有运维和额度成本

## 针对你当前诉求的结论

先做纯前端本地版最合适。  
后续如果你希望“多设备同步/长期云备份”，再接免费层后端即可。

## MiniMax 额度使用建议

- 默认不调用模型
- 仅在自然语言解析失败或歧义较高时调用
- 每次调用前显示“将使用云解析”提示，可手动确认
